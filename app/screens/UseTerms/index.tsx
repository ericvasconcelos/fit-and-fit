import * as React from 'react';
import { Text, View } from 'react-native';
import { styles } from './styles';

export default function UseTerms() {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>UseTerms</Text>
    </View>
  );
}
